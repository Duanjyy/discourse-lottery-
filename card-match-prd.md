# 产品需求文档 (PRD)：极简风分层三消小游戏《卡片叠叠乐》

## 1. 产品概述
**1.1 产品背景**
当前市场上的休闲小游戏（如《羊了个羊》）深受用户喜爱，其核心玩法“分层遮挡+底部卡槽消除”具有极强的魔性和成瘾性。本项目旨在通过纯前端技术，零后端依赖地复刻这一核心机制，打造一款即开即玩、极简解压的单机网页小游戏。

**1.2 产品定位**
一款无需联网、打开即玩、自动存档的休闲解压消除游戏。目标用户为需要短暂放松、喜欢轻度烧脑挑战的玩家。

## 2. 核心玩法机制
**2.1 游戏牌局（棋盘）**
*   **卡牌分层**：卡牌以3D堆叠的形式分布在牌局中，分为多个层级。
*   **遮挡逻辑**：位于下层的卡牌如果被上层卡牌在物理坐标上覆盖，则处于“被遮挡”状态（UI上表现为置灰或降低透明度），此时不可点击。
*   **解锁条件**：只有当压在某张卡牌上方且有重叠区域的所有卡牌都被移走后，该卡牌才解锁并恢复可点击状态。

**2.2 卡槽机制**
*   **卡槽容量**：屏幕底部设有一个固定容量的卡槽（初始为7格，极难模式下可降为5-6格）。
*   **入槽规则**：玩家点击牌局中“可点击”状态的卡牌，该卡牌会从牌局飞入底部卡槽。
*   **自动排序**：进入卡槽的卡牌，如果是相同图案，会自动排列在一起，方便玩家识别。

**2.3 消除与判定**
*   **消除触发**：当卡槽内集齐 3 张相同图案的卡牌时，触发消除。这三张卡牌从卡槽中移除，释放卡槽空间。
*   **游戏胜利**：牌局上的所有卡牌全部被消除，且卡槽清空。
*   **游戏失败**：卡槽被填满（达到容量上限），且没有满足 3 张相同的消除条件。

## 3. 功能模块说明

**3.1 首页模块**
*   **游戏名称**：显示《卡片叠叠乐》及极简标语。
*   **数据展示区**：展示玩家当前收集的“碎片”数量和“总通关数”。
*   **开始/继续闯关**：
    *   如果当前没有未完成的牌局，显示“开始闯关”，点击进入对应进度的关卡。
    *   如果存在未完成的牌局，显示高亮的“继续闯关”，点击直接恢复未完成的残局。

**3.2 游戏核心模块**
*   **顶部信息栏**：显示当前关卡数、返回首页按钮、全局音量控制（静音/开启）以及本局重置按钮。
*   **牌局渲染区**：居中展示当前关卡的卡牌堆叠状态。卡牌图案为各种水果、美食（Emoji）。
*   **道具系统（可选扩展）**：在卡槽上方提供基础道具（如：移除卡槽内的一张卡牌、洗牌重新打乱未收集的卡牌、随机提示可点击的卡牌等）。

**3.3 结算模块**
*   **通关结算**：游戏胜利时，弹出毛玻璃质感的胜利弹窗，展示撒花动效，提供“下一关”和“返回首页”按钮。并发放相应的碎片或积分奖励。
*   **失败结算**：游戏失败时，弹出失败提示框，提供“重新开始本关”和“返回首页”按钮。

## 4. 难度与数值设定
*   **关卡设计**：采用算法动态生成关卡，支持无尽模式（如999关）。
*   **难度递增策略**：
    *   **卡牌总数**：随关卡数递增（始终保持为3的倍数）。
    *   **最大层数**：随关卡数递增，导致遮挡关系更复杂。
    *   **图案种类**：随关卡数增加，种类越多，凑齐3张的概率越低，极度考验卡槽管理。
    *   **卡槽限制**：在极高关卡（如100关以后），卡槽容量从7格缩减为6格或5格。

## 5. 交互与表现需求
*   **动效反馈**：
    *   卡牌入槽需有平滑的位移动画。
    *   卡牌消除时，不可生硬消失，需增加“Pop”（放大后瞬间缩小至消失）的动效。
*   **音效反馈**：
    *   **点击音效**：清脆的按键声（如吐泡泡声）。
    *   **消除音效**：具有解压感的脆响（如砸玻璃声或咔嚓声）。
    *   **背景音乐**：轻松休闲的 BGM。
*   **UI 风格**：极简现代风，大量使用留白、圆角（如 rounded-2xl）、浅色阴影（shadow-sm）和毛玻璃效果（backdrop-blur）。禁用网页上下滑动回弹（touch-none, overflow-hidden）。

## 6. 技术实现要求（提供给 AI 开发者的核心约束）
1.  **技术栈**：React + Vite + TailwindCSS + Zustand。
2.  **纯前端架构**：**绝对禁止引入任何后端接口或数据库。** 必须作为纯静态项目运行。
3.  **数据持久化**：所有的关卡进度、卡牌残局状态、收集的碎片等数据，必须通过 Zustand 的 `persist` 中间件实时保存在浏览器的 LocalStorage 中。
4.  **音频处理**：使用 `use-sound` 库处理音频播放，考虑到浏览器自动播放策略，需在用户首次交互后激活音频上下文。音频文件必须支持本地加载（`/public/sounds/`）以防跨域和防盗链。
5.  **部署构建**：支持通过 `npm run build` 打包为纯静态的 HTML 文件夹，双击 `index.html` 即可在本地无服务器环境下运行（需配置路由为 HashRouter 及基础路径相对化）。
# 【Hello AI 科技致善】用 SOLO 打造零门槛纯本地的高清简历生成器，助力大学生跨越求职第一步

### ①摘要：
面向**缺乏求职经验、不会排版简历的迷茫大学生**，在**求职准备和简历制作场景**下提供**零门槛、多模板切换、所见即所得且绝对保护隐私的纯前端简历生成器**。目前通过本地测试和导出演示验证，该工具能稳定输出无偏移的高清 PDF 简历，帮助学生省去排版烦恼，专注打磨求职内容。

---

### ②真实场景与需求：

- **目标人群**：即将步入职场、缺乏经验且对求职感到迷茫的在校大学生或应届毕业生。
- **痛点描述**：大学生在找第一份实习或工作时，往往连“简历该长什么样”都不清楚。网上虽然有很多简历模板，但往往需要充值、注册账号，或者导出时带有水印；很多在线平台还会强制要求上传个人隐私数据（电话、邮箱、家庭住址），存在隐私泄露的风险。学生们在排版上耗费了大量时间，甚至因为使用了不规范的模板而在初筛时被淘汰。
- **现有做法**：使用 Word/WPS 套用传统模板，排版容易错乱且效率极低；或者花钱在第三方简历网站购买带水印的模板，不仅成本高，还面临隐私数据被滥用的风险。现有的免费工具往往在导出 PDF 时存在严重的排版错位问题，极不稳定。

---

### ③作品介绍：
**「极简纯本地简历生成器」**是一个完全基于浏览器的纯前端 Web 应用。

**核心功能**：
1. **纯本地离线运行**：无需注册登录，没有任何后端服务器，所有填写的个人数据只保存在浏览器的 `localStorage` 中，绝对保护大学生的隐私安全。
2. **多场景专业模板**：内置了「简约」、「商务」、「校园」、「技术风」等四套经过专业打磨的简历排版模板，一键无缝切换，学生无需操心排版。
3. **所见即所得**：左侧表单编辑，右侧实时渲染 A4 比例的简历预览，支持调整字号、行距、单列排版等细节。
4. **高质量无偏移导出**：提供高清 PDF 和 PNG 图片两种一键导出方式。彻底解决了传统截图转 PDF 容易出现的文字向下偏移、错位等顽疾，输出真正干净、专业的求职简历。
5. **智能示例填入**：一键生成各行各业（如前端开发、UI 设计、产品运营等）的优秀简历示例，给迷茫的大学生提供直接的内容参考。

---

### ④用 SOLO 实现的过程：

在开发这个工具时，我完全依靠了 TRAE SOLO 的全自动编码能力，整个过程就像是指挥一个经验丰富的全栈工程师在干活：

- **任务拆解**：
  1. 首先让 SOLO 使用 Vite + React + TailwindCSS 搭建出工程骨架。
  2. 设计数据流：通过 Zustand 实现全局状态管理和 localStorage 的持久化。
  3. 页面布局：切分布局，左侧是可折叠的表单编辑区（支持基础信息、教育经历、工作经验、项目等多个模块的动态增删），右侧是固定 A4 比例的实时预览区。
  4. 模板引擎：让 SOLO 根据不同职业需求，写了四套不同风格的 React 简历模板组件。
  5. 核心难点攻坚：重点解决前端导出 PDF 的清晰度和排版偏移问题。

- **用了 SOLO 哪些能力**：
  - **自动分析与架构**：只用了一句自然语言，SOLO 就帮我建好了 Zustand store 和完整的 TypeScript 类型定义。
  - **组件化重构**：当页面代码越来越长时，SOLO 主动帮我把冗长的代码拆分成了 `PreviewPane`、`EditorToolbar` 等独立组件。
  - **Bug 定位与自我修复**：这是最惊艳的一点。在处理 PDF 导出时遇到了极大的困难，SOLO 通过读取控制台日志，不断自我排查、查阅文档，甚至在我只给出一句“白屏了”的反馈下，自己找到了深层空指针异常的原因并修复。

- **中间踩过什么坑（及 SOLO 的神级表现）**：
  - **坑 1：导出 PDF 文字下移错位**。一开始用的是传统的 `html2canvas` 方案，但在带有滚动条的复杂页面里，截出来的 PDF 文字总是往下掉。SOLO 尝试了强制滚动归零、临时挂载绝对定位等多种方法，最后果断提议更换为基于 SVG `<foreignObject>` 渲染引擎的 `html-to-image` 库，彻底根除了这个业内著名的玄学 Bug。
  - **坑 2：持久化数据结构污染导致白屏**。在开发“一键生成随机示例”功能时，因为老的数据结构留在了本地缓存里，导致读取某个不存在的 `visible` 属性时 React 整个崩溃白屏。SOLO 迅速反应，不仅给所有模板加上了可选链（`?.`）保护，还在 Zustand 的 `migrate` 函数里写了一个深度归一化（normalize）逻辑，保证旧数据启动时能被自动修复补全。

---

### ⑤成果展示：

这个简历生成器已经可以完美在本地运行。没有任何复杂的后端依赖。

- **代码仓库**：[在此填入你的 GitHub 仓库链接]
- **Web 应用体验**：[在此填入你的 Vercel/Netlify 部署链接，如果没有可以写“克隆代码后 `npm run dev` 即可秒开”]

**核心界面截图**：
*(建议在发帖时在这里插入两张截图：一张是左右分栏的编辑界面，一张是导出的高清 PDF 效果图)*

---

### ⑥验证方式与下一步（加分项）

**当前验证**：
- **模拟测试验证**：已通过内置的多套职业示例数据（包含前端开发、UI 设计师、产品运营）验证了各个模板在文字极多、极少极端情况下的排版稳定性。
- **跨平台导出验证**：在 Windows 和 Mac 浏览器环境下，导出的 PDF 与图片均能保持 1:1 的所见即所得效果，无任何水印和偏移。

**下一步计划**：
1. **AI 简历润色接入**：未来希望接入本地大模型或者 API，当学生不知道怎么写“项目经历”时，可以根据学生输入的几个关键词，用 AI 自动扩写、润色成符合 STAR 法则（情境、任务、行动、结果）的专业描述。
2. **更多行业模板**：收集更多真实大厂 HR 喜欢的简历排版，增加“金融”、“考公”、“新媒体”等细分垂直领域的模板。
3. **校园推广**：希望能将这个纯净、安全的开源工具推荐给校园就业指导中心，帮助更多学弟学妹跨出求职路上的第一步。
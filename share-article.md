# 零代码手搓了一个“羊了个羊”平替，我想聊聊 AI 编程到底能干啥

前几天手痒，脑子里一直盘旋着那种“下面七个槽，上面一堆牌”的消除游戏。想着自己能不能也搞一个，换成极简的 UI，加上那种特别解压的“咔嚓”音效。

但现实很骨感。我并不是什么前端大佬，真要自己去算 3D 卡牌重叠遮挡关系、搞状态管理、配置路由，估计还没等写出核心逻辑，就已经被 React 的各种环境配置劝退了。

正好最近试了试 TRAE SOLO，我萌生了一个想法：**如果我完全不写代码，纯靠跟 AI 聊天，能不能凭空捏一个游戏出来？**

结果，我还真做出来了。这个叫“卡片叠叠乐”的小游戏不仅能跑，而且还挺完整。

---

## 我是怎么“使唤” AI 的？

这次最大的经验是：**不要把 AI 当成带了界面的搜索引擎，你要把它当成一个真的打工人。**

**第一步：不急着敲代码，先要策划案。**
我没有一上来就跟它说“给我写个消消乐”，而是先用 MTC (More than Coding) 模式，把自己想要的核心规则（底层遮挡不可点、卡槽满7个失败、凑齐3个消除）告诉它，让它帮我写一份详细的 PRD。这很重要，因为只有规则明确了，AI 才不会乱发挥。

在这份 PRD 里，我还特意给它立了个规矩：“必须是纯前端单机版，不依赖任何后端，拔了网线也能玩。”

**第二步：丢给 SOLO 模式，让它出方案。**
拿到 PRD 后，我直接切到写代码的模式。SOLO 看了文档，先给我输出了技术架构，定了 React + Vite + Zustand。我看大方向没问题，它才开始自己建文件夹、敲命令初始化项目。

**第三步：像个事儿逼产品经理一样提需求。**
游戏跑起来后，难免有粗糙的地方。这时候就可以直接用大白话提要求了：
- “点击三个一样的卡片，加个 Pop 动画，别直接一闪就没了。”
- “重置按钮一开始说只能点三次，现在我觉得太憋屈了，把限制去掉，顺便把弹窗美化得高级一点。”

它完全能听懂这种非常主观和情绪化的需求。不仅帮我重构了渲染逻辑加了动画，还自己配了个毛玻璃效果的通关弹窗，审美非常在线。

---

## 踩过最大的坑：防盗链与本地音效

整个过程中最让我印象深刻的，是处理游戏音效的时候。

一开始，我让它加上背景音乐和点击音效。结果游戏跑起来死活不出声。我问它：“怎么没有音效？”

它自己去翻了终端和浏览器的报错日志，发现原来是引用的在线音源被网站防盗链给拦截了。换做以前，我肯定要去疯狂搜“如何解决音频跨域”。结果它二话不说，直接在后台跑 `curl` 命令，把那些音效文件全下载到了本地目录下，然后自己改了代码路径。

后来我自己找了两个本地音频，一个叫“吐泡泡.mp3”，一个叫“砸玻璃.mp3”，直接丢给它，让它替换成点击和消除的声音。它也完美接管了，整个游戏的听觉反馈瞬间变得极其解压。

---

## 最后想说的

折腾到最后，我让它帮我把整个项目编译成了 HTML，还专门叮嘱了一句：“网页固定住，不要能上下滑动。”它不仅改了全局 CSS，连路由都换成了适合静态文件跑的 HashRouter，打包出了一个干干净净的文件夹。双击里面的 `index.html` 就能直接玩，里面实打实地跑着 999 个自动生成的动态关卡，还有 LocalStorage 自动存档。

如果是以前，光是看文档、算遮挡坐标、解决音频播放限制，对我来说可能得耗上两个星期。但这次，连提需求带来回扯皮改 UI，一共就花了一个小时左右。

经过这次折腾，我算是看明白了。以后写代码的门槛只会越来越低，重要的是你有没有把想法拆解成逻辑的能力。只要你的脑子够清楚，AI 真的是个任劳任怨的全栈合伙人。
# 【Code With SOLO】用SOLO搓一个小学语文题目生成器

## 1. 摘要

在教育类内容生产里，最费时间的往往不是“出题”，而是“把题变成一张像样的试卷”：A4 分页、题型混排、留白格式（田字格/四线三格/横线）、答案怎么处理、最后还得能直接打印或导出 PDF。为了解决这个真实痛点，我用 TRAE SOLO 做了一个「小学语文练习卷生成器」，可以按年级和难度生成常见题型，支持智能分页、多页 A4 预览、答案附后，以及一键导出和打印。

这个项目不是那种“看起来很炫但用不上”的大项目，而是一个我愿意长期维护、也能给老师/教培/内容运营直接拿去用的工具：打开页面 → 选题型/题量 → 生成 → 打印。整个流程尽量贴近一线使用者的实际操作习惯。

> （插图建议 1：项目首页整体效果：顶部栏 + 预览区两列 A4）

---

## 2. 背景：为什么我想做这个工具

继上次用 SOLO 写了一个小学数学题生成器之后，我发现这类“高频、刚需、但总被排版拖慢”的小工具，特别适合用 AI 来做。一方面需求很明确：题要对、卷要像、能直接打印；另一方面细节又多：题型、格式、分页、答案、导出……手工做很容易把时间耗在重复劳动上。

所以这次我干脆趁热打铁，又用 SOLO 做了一个「小学语文题目生成器」。目标也很朴素：让老师/教培/内容团队能在几分钟内生成一份“像真的试卷一样”的语文专项练习卷，而不是把题堆在网页上看一眼就算完。

和数学题生成器相比，语文这边的难点更偏“排版与规范”。语文题不像数学那样结构统一，题目形态包含拼音、汉字书写格、长句子、病句修改、成语填空、仿写句子等——每一种题型都对应一套更符合小学语文练习习惯的呈现方式。我这次也把重点放在这些“真实可用”的细节上：比如拼音必须是带音调符号的，而不是数字；写汉字更适合田字格，注音更适合四线三格；仿写句子不能给一串长括号糊弄，必须留出横线作为书写区；题多的时候必须自动分页成多张 A4，否则导出和打印都会崩。

总的来说，这个语文题目生成器延续了上一个项目的思路：先把“生成 → 预览 → 导出/打印”的闭环做通，再一条条打磨体验，直到工具真的能在家庭或课堂里拿来就用。也因此我更确定了一件事：SOLO 最适合的不是做炫技项目，而是把这些原本需要 1-2 小时反复排版、调整、导出的工作，压缩到“点几下就完成”的程度。

我平时会遇到一个非常具体的场景：需要把“语文专项训练”快速落地成可打印的 A4 练习卷，交付给老师、教培同学或内容团队直接使用。如果靠手工，最常见的流程是：到处找题、复制粘贴进 Word、调字号、调行距、加括号、加横线、调分页……改到最后往往还会遇到“下一页只剩两行题但空白很大”“题号断了”“田字格对不齐”等等细节问题。

这件事最让人崩溃的地方在于：明明只是一个常见的交付需求，却要先花 1-2 小时在排版上。于是我决定做一个小工具，把“出题 + 排版 + 导出”这件事一次性自动化。

---

## 3. 实践过程：我怎么用 SOLO 把项目做出来的

整个开发流程我刻意分成两段：先“想清楚再动手”，再进入编码落地。具体做法是：我先用 SOLO 的 MTC（More Than Coding）模式把 PRD 产出出来，把目标用户、题型范围、页面结构、导出方式、关键体验点（A4 分页、留白格式、答案策略等）写清楚；PRD 确认后再把需求交给 SOLO 进入开发阶段，按模块逐步实现并在浏览器里边跑边改。这样做的好处是，后面不容易在细节里迷路，每一次迭代都能对照 PRD 判断“是不是更接近真正可用”。

### 3.1 先做闭环：能生成、能预览、能导出/打印

我做的第一件事不是追求题库多大、题型多复杂，而是先把闭环跑通：配置题型 → 生成题目 → 预览 A4 → 一键打印 / 导出 PDF / 导出图片。这个闭环一旦跑通，后面才有意义去谈体验优化。

这个阶段 SOLO 最有帮助的是“快速成型”：搭页面、做状态管理、把导出能力串起来。做完之后我立刻用实际场景测试：生成 60~100 道题，看 PDF 是否清晰，打印是否能直接分页，答案是否能按需求附后。

> （插图建议 2：打印/导出按钮区域 + 导出的 PDF 多页效果）

---

### 3.2 真正难的部分：智能分页，把题拆成多张 A4

如果只在网页里把题目往下堆，那很简单；但要“像试卷一样分页”，事情就复杂了。不同题型高度不一样，同一题型在不同列数下高度也不一样，还要考虑标题、上下边距、行间距、跨页续题等情况。

我最终采用的做法是：在页面里放一个“不可见的测量容器”，用 DOM 实际渲染每个题目、读取这些题目的真实高度，然后根据 A4 可用高度进行打包分配。这样分页不是凭感觉估算，而是由真实渲染尺寸决定，稳定很多。

这个过程中我踩过不少坑，比如：

预览区看起来分页正常，但导出 PDF 时页高不一致，后来发现测量容器与真实 A4 的宽度/样式不一致会引入误差。开启“分组标题 + 多列布局”后，某些题目会在列间造成高度计算偏差，需要按列高度取最大值来做分页判断。页底空白偏大的时候，视觉 padding 和分页可用高度的计算必须同步调整，否则会出现“页面视觉变紧，但分页算法仍按旧高度分配”的错位。

SOLO 在这里的价值很明显：我每次提出一个“看起来不对”的现象，SOLO 能快速定位到可能是样式层、测量层还是算法层的问题，并给出可落地的改法。然后我用一份长试卷重复测试，直到分页稳定。

> （插图建议 3：一份题量较大的试卷，多页 A4 自动分页的预览）

---

### 3.3 题型细节：拼音显示、格子规范、长文本更易写

工具要“能用”，细节就不能糊弄。比如：

**拼音题**：我一开始用的拼音库默认输出带数字的声调（例如 `pin1 yin1`），这在作业里看起来很别扭。我后面改成输出带声调符号的拼音（例如 `pīn yīn`），更符合小学语文的习惯。

**看拼音写汉字 vs 生字注音**：这两个题型的留白格式其实不同。写汉字更适合田字格；注音更适合四线三格。我把这两个题型做成“强制格式”，不受全局辅助线设置影响，避免用户选错导致试卷看起来不专业。

**长文本题**：像“修改病句”“关联词填空”等，文字偏长时，如果字距太紧会影响阅读和书写。我给这类题加了更舒适的行高和字间距，并且对括号填空（如 `(    )`）做了明显的视觉强化：括号更大、间距更宽，更接近纸质练习卷的书写习惯。

> （插图建议 4：拼音题（声调符号）、田字格/四线三格对比、长文本括号更明显的效果）

---

### 3.4 仿写句子：去掉长括号，改成真正的横线书写区

“仿写句子”这个题型最典型的糟糕排版就是：`仿：(                                                                      )` 一长串括号，看起来不美观，写起来也别扭。

我把这一题型改成了更像纸质练习册的形式：题干只保留 `仿：`，下面给两行横线作为书写区，并且根据反馈把横线位置往上挪了一点，让横线更贴近“仿：”的视觉重心，写起来更自然。

这里有一个很现实的点：你以为改了渲染就行，但如果生成题干里还残留括号，还是会显示出来。所以我同时改了“生成逻辑”和“渲染逻辑”，确保括号从源头消失。

> （插图建议 5：仿写句子题型：`仿：` 下方两行横线的效果）

---

### 3.5 UI 重构：不做左右分栏，改成沉浸式预览 + 全屏配置抽屉

我最开始做的是经典的“左边配置、右边预览”，但用久了会觉得：预览才是核心，配置应该是“随时可调但不抢空间”。

所以我把布局重构成：

页面顶部改成简洁的快捷栏，用来切换年级、难度并一键生成。题型选择、排序和排版参数统一收纳进“高级配置”，点击后以全屏覆盖的侧边抽屉打开。预览区保持沉浸式展示 A4，并在大屏场景支持一行显示两张 A4，减少频繁滚动与翻页确认的成本。

这个改动看似只是 UI，但会牵扯很多细节：比如预览区滚动失效、弹层打开后页面不能滚动、抽屉关闭要恢复 body overflow、按钮位置不遮挡内容等。我在重构过程中也遇到了 JSX 标签闭合错误导致页面白屏，这类问题 SOLO 根据报错行号定位非常快，但也提醒我：做 UI 重构一定要小步提交、频繁刷新验证。

> （插图建议 6：全屏抽屉式“高级配置”界面；插图 7：大屏一行两张 A4 的预览效果）

---

## 4. 成果展示：现在能做什么

目前项目的核心能力包括：

支持按年级与难度生成题目，并且可以勾选题型、拖拽排序。每个题型支持配置列数与题数（平分或固定），同时提供总题数、行间距、辅助线样式、连续题号等全局排版参数。生成结果会自动智能分页拆分成多张 A4，答案支持不附答案、题后标红和答案附后单独成页三种模式，并且支持打印、导出 PDF、导出图片。预览体验在大屏场景可实现一行两张 A4，提高检查效率。

如果需要外链，我建议这样放（社区不支持视频/压缩包）：

GitHub 仓库：`（你填仓库链接）`。在线演示：`（你填演示链接，没有也可以不填）`。使用说明：`（可选）`。

---

## 5. 效果与总结：我觉得 SOLO 最适合用在这种“真实但琐碎”的工作里

这次做完我最大的感受是：比赛不需要“多复杂”，反而这种“真实会用、能落地”的工具更容易坚持做下去。

如果只用传统方式，这种项目很容易卡在两个地方：一是起步成本（搭架子太慢），二是细节迭代（每次改一点都要自己排查很久）。SOLO 在我这里扮演了一个很明确的角色：SOLO 不替我做决定，而是帮我把决定快速落地——并且在出现问题时，能把排查路径缩短到“几个可能的文件/组件”。

更重要的是，我在这个过程中形成了一套可复用的工作方法：先闭环、再打磨体验；每次只优化一个用户能感知的点；把“试卷感”的细节当成第一优先级。以后不管做什么小工具，这套节奏都可以直接套用。

---

## 6. 附：我实际迭代过的一些“用户能感知的小点”

这些点看起来不大，但每个都能让成品更像真实应用，而不是 demo：

拼音声调从数字改为音调符号；成语补充题去掉题目里自带的答案；去掉每题重复前缀（例如“补充成语：”）让试卷更干净；A4 底部留白做了调整让页面更紧凑；仿写句子从长括号改为横线书写区并微调横线高度；预览区从单列改为大屏两列提高浏览效率；高级配置从分栏改成全屏抽屉，让预览更沉浸、配置更集中。

> 如果你对某个细节也有强迫症（比如“括号再大一点”“横线再靠上一点”“A4 页边距再小一点”），欢迎评论交流，我这次几乎把这些坑都踩了一遍。
